---
import SearchBox from './SearchBox';
---

<section class="hero-pattern py-24 md:py-32 px-6 border-b-2 border-sea-glass">
  <div class="max-w-[1440px] mx-auto grid lg:grid-cols-12 gap-16 items-center">
    <div class="lg:col-span-7">
      <h2 class="font-serif text-5xl md:text-7xl mb-8 leading-[1.1] text-white">
        Tu hogar en el <span class="italic text-sea-glass">Caribe</span>, encontrado como una postal
        antigua.
      </h2>
      <div class="max-w-xl mb-12">
        <p class="text-lg leading-relaxed text-white/80 font-serif italic">
          Descubre propiedades con alma en los barrios más icónicos de Cuba. Desde casas coloniales
          en el Vedado hasta villas frente al mar en Miramar.
        </p>
      </div>

      <!-- Search Box - Isla React para interactividad -->
      <SearchBox client:load />
    </div>

    <!-- Imagen solo visible en desktop (lg+) -->
    <div class="lg:col-span-5 hidden lg:block">
      <div class="relative">
        <div
          class="absolute -top-10 -right-10 z-10 w-32 h-32 bg-white border-2 border-dashed border-sea-glass flex items-center justify-center rotate-12 shadow-lg"
        >
          <div class="text-center">
            <div class="text-[10px] font-bold text-primary uppercase leading-tight">Auténtico</div>
            <div class="text-[10px] font-bold text-sea-glass uppercase leading-tight">Estilo</div>
            <div class="text-[10px] font-bold text-primary uppercase leading-tight">Cubano</div>
          </div>
        </div>
        <img
          alt="Calle colorida de La Habana"
          class="w-full h-[500px] object-cover postcard-border grayscale-[20%] sepia-[10%]"
          src="/images/hero-havana.jpg"
          loading="eager"
          decoding="async"
        />
      </div>
    </div>
  </div>
</section>
